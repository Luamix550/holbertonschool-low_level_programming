#include <stdio.h>

/**
 * main - Entry
 *
 * Description Prints a string to stdout
 * if return 0 (Success!)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
