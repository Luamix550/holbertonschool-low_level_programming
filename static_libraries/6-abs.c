#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @r: point entry
 *
 * Return: absolute value of an interger
 */

int _abs(int r)
{
	int c;

	c = abs(r);

	return (c);
}

