#include "main.h"
/**
 * mul - multiples two intergers
 * @a:get a first number
 * @b:get a second number
 * Return: returns the value of the operation
 */
int mul(int a, int b)
{
int product;

product = a * b;
return (product);
}
