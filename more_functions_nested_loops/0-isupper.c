#include "main.h"
/**
 * _isupper - evaluates that the number is in capital letters
 * @c: get number
 * return: 1 if the number is uppercase
 * return: 0 if not
 */
int _isupper(int c){

    if (_isupper(c))
    {
        return (1);
    }
    else{
        return (0);
    }
}
