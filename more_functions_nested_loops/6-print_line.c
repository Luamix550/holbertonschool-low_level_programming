#include "main.h"
/**
 *print_line - prints an underscore depending on the number given to n
 *
 * @n: get a number
 */
void print_line(int n)
{
	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i;
		for (i = 0 ; i < n ; i++)
	{
	_putchar('92');
	}	
	_putchar('\n');
}
}
